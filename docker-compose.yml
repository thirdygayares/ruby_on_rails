services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    # no entrypoint here
    command: ["bundle","exec","rails","server","-b","0.0.0.0","-p","3000"]
